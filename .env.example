# ============================================================================
# XHS Intelligence Agent v5.0 - 环境变量配置模板
# ============================================================================
# 使用方法:
# 1. 复制此文件为 .env
# 2. 填入你的真实配置
# 3. .env 文件已被 .gitignore 忽略，不会上传到 Git
# ============================================================================

# AI API 配置 (可选，不填则使用默认值)
# 支持 OpenAI 兼容的 API 接口
AI_API_BASE=https://api.openai.com/v1
AI_API_KEY=sk-your-api-key-here
AI_MODEL=gpt-3.5-turbo

# 代理配置 (可选，用于网络受限环境)
# 示例: http://127.0.0.1:7890
# HTTP_PROXY=
# HTTPS_PROXY=

# ============================================================================
# 注意事项:
# - API_KEY 是敏感信息，请勿泄露
# - 如果使用代理 API (如 yinli.one)，请将 AI_API_BASE 改为代理地址
# - 默认使用 gemini-2.5-flash 模型，可改为 gpt-4o 等
# ============================================================================
